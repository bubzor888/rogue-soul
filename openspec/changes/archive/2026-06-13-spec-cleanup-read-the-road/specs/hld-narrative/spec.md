## MODIFIED Lines

**Re-tag** the visual direction annotation from `[OPEN·MVP1]` to `[OPEN·MVP2]`. Visual and art direction is a UI/presentation concern that does not block the headless MVP1 run; it is required before the playable MVP2 release.

Change:

> `` `[OPEN·MVP1]` Specific visual direction for each floor register to be defined in a UI/art direction session. ``

To:

> `` `[OPEN·MVP2]` Specific visual direction for each floor register to be defined in a UI/art direction session. ``

**Also re-tag to `[OPEN·MVP2]`** (narrative is not needed for headless engine testing):
- `` `[OPEN·MVP1]` Specific guardian dialogue per vessel to be written in `lld-narrative`. `` → change to `[OPEN·MVP2]`
