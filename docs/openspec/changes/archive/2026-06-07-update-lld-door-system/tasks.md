## 1. Update lld-door-system spec

- [x] 1.1 In `LLD-FLOOR-DOOR-001`: generalise the single-door exception — remove the specific Worn Map reference; rewrite as "unless an item explicitly forces a single-door beat" with `LLD-FLOOR-BEATS-003` as an example; add a second scenario for the item-forces-single-door case
- [x] 1.2 In `LLD-FLOOR-DOOR-003`: add that different non-combat types are visually distinguishable from each other (distinct symbols); retain the existing scenario for subcategory content hidden; add a second scenario showing that Memory Fragment vs Wandering Soul are distinguishable
- [x] 1.3 In `LLD-FLOOR-DOOR-004`: rename title from "Forced-Combat Both-Doors Rule" to "Pool Exhaustion Both-Doors Rule"; rewrite body around pool exhaustion (any type removed from pool won't appear; remaining fill may produce same-type both doors); replace existing scenario with two new scenarios (both doors same type after exhaustion; both doors combat after non-combat exhaustion)

## 2. Archive

- [x] 2.1 Archive the `update-lld-door-system` change
